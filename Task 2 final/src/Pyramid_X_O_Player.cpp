#include "../include/Pyramid_X_O_Player.h"

Pyramid_X_O_Player::get_move(int &x, int &y) {
    cout << "\nPlease enter your move x and y from the shown cells separated by spaces: ";
    cin >> x >> y;
}